# Свод правил

- **Область.** `docs/rules/**` и `.claude/skills/**` — сам свод, его доставка и порядок ведения.
- **Читать перед.** Правкой любого файла свода, добавлением нового правила или файла; при выборе,
  какой файл свода открыть под задачу.
- **Словарь.** Плейсхолдеры и модальность — здесь, ниже.

## Как пользоваться

Правила обязательны к соблюдению. Файл открывается по триггеру из его пункта
**Читать перед** — до правки соответствующего слоя, а не после.

Всегда в контексте — только `20-agreements.md` (импорт в `CLAUDE.md`) и карта свода оттуда же.
Остальные файлы подтягиваются по задаче: у каждого есть одноимённый skill, который велит
прочитать файл целиком. Skill выбирает модель — если правишь слой, а свод не подтянулся,
читай файл сам.

При расхождении источников приоритет такой:

| Приоритет | Источник | Что задаёт |
|---|---|---|
| 1 | `CLAUDE.md` | язык, тон, формат ответа, команды сборки |
| 2 | файл свода | нормы слоя |
| 3 | код | как есть сейчас; расхождение с правилом — дефект кода, а не правила |

## Карта свода

| Файл | Skill | Тема | Читать перед |
|---|---|---|---|
| `10-architecture.md` | `architecture` | границы библиотеки, `Core.Config`, способы получения зависимостей | правкой конфигурации, адаптеров брокеров, границы HTTP; любой ссылкой на потребителя |
| `11-domain.md` | `domain` | `Prim`, `Enum`, `Codec`, `Context`, `Es.Event`, `Version` | новым Prim / Enum / кодеком, правкой агрегата или профиля Codec |
| `12-errors.md` | `errors` | `%Error{}` (`:domain` / `:app`), каталоги ошибок, cause-цепочки | введением нового кода ошибки, работой с чувствительными данными |
| `13-repos.md` | `repos` | `Repo` behaviour / `Repo.Pg` / `Schema` / `Specs`, read vs write | новым репозиторием, схемой, View; правкой `use Repo.Pg*`, `constraint_errors` |
| `14-events-outbox.md` | `events-outbox` | domain events, flush в одной TX, outbox lifecycle | новым событием, правкой outbox, разбором записей в `:failed` и DLQ |
| `17-otp-concurrency.md` | `otp-concurrency` | дерево процессов, `init/1` / `handle_continue`, таймауты, mailbox | новым GenServer или supervisor'ом, правкой таймаутов, backoff, shutdown |
| `19-testing.md` | `testing` | case-модули, round-trip кодеков, golden-фикстуры событий | новым тестом, тестом репозитория, кодека, процесса, Enum |
| `20-agreements.md` | — (всегда) | CQS, логирование, `@spec` / `@doc`, guards, алиасы, safe vs bang | любой правкой кода: оформление модуля, имена, спеки, логи, стиль |
| `21-observability.md` | `observability` | метрики / трейсы / логи, `Core.Otel`, где ставить span | новой метрикой или span'ом, правкой `Core.Otel` и словарей semconv |

`DEBT.md` — реестр осознанных отступлений от сводов. Сверяться с ним, прежде чем «чинить»
найденное несоответствие.

Нумерация сквозная для всех сводов проекта. Номера 15, 16, 18 заняты сводом
приложения-потребителя (`15-web-api.md`, `16-caching.md`, `18-migrations.md`) — в этом
репозитории их нет, ссылки на них помечаются «(свод приложения)».

## Доставка: скиллы

Файл — источник истины, skill — только доставка. Правки вносятся в `docs/rules/*.md`;
`SKILL.md` трогать нужно, только если поменялось имя файла или область применения.

- имя скилла = имя файла без номера и расширения (`13-repos.md` → `repos`); иначе связь
  «свод ↔ skill» держится на памяти, а не на проверке;
- `description` скилла MUST перечислять содержимое свода и заканчиваться триггерами
  «Использовать при …» — по нему модель и решает, грузить ли файл;
- тело `SKILL.md` — указатель на файл и на `DEBT.md`, не пересказ правил: пересказ разойдётся
  с источником;
- у `20-agreements.md` скилла нет: он всегда в контексте импортом из `CLAUDE.md`.

Проверяется: `make rules-check` — у каждого свода есть skill с ожидаемым именем, непустым
`description` и ссылкой на свой файл.

## Плейсхолдеры

| Плейсхолдер | Значение |
|---|---|
| `MyApp` | корневой namespace приложения-потребителя |
| `:my_app` | его OTP app atom |
| `<BC>` | bounded context |
| `<Actor>` | actor / role-срез |
| `<Aggregate>` | агрегат |

## Модальность

Нормы записываются латиницей заглавными; всё остальное в тексте — пояснение, а не норма.

| Слово | Значение |
|---|---|
| MUST | обязательно; нарушение — дефект |
| MUST NOT | запрещено |
| SHOULD | по умолчанию так; отступление требует причины на call site |
| SHOULD NOT | так не делать без причины |
| MAY | допустимо, выбор за автором |

Расплывчатые формулировки («по возможности», «желательно») MUST NOT заменять модальность:
правило либо норма, либо пояснение.

## Шаблон файла правил

````markdown
# Репозитории

- **Область.** `lib/core/repo/**`, `lib/core/es/**`; у потребителя — `<role>/<aggregate>/repo*`.
- **Читать перед.** Новым репозиторием или Ecto-схемой; правкой `use Repo.Pg` / `Repo.Pg.Es`.
- **Словарь.** Плейсхолдеры и модальность — `00-index.md`.

## <Тематический блок>

Норма формулируется через MUST / MUST NOT / SHOULD / SHOULD NOT / MAY.

Проверяется: `mix credo --strict` (`Credo.Check.Readability.StrictModuleLayout`).

### <Подтема>

```elixir
# плохо — <причина>
...

# хорошо
...
```

## Связанные правила

- Ошибки — `12-errors.md`
- Тесты — `19-testing.md`
````

Оформление, общее для всех файлов свода:

- шапка — список из трёх пунктов (`- **Область.**`, `- **Читать перед.**`, `- **Словарь.**`);
  подряд идущие строки Markdown склеивает в один абзац, поэтому ключи MUST быть пунктами,
  а перенос уходит под отступ;
- заголовки: один H1, блоки — H2, подтемы — H3; H4 MUST NOT;
- у каждого fenced-блока MUST быть указан язык (`elixir`, `bash`, `markdown`, `text`);
- строка `Проверяется: …` ставится только там, где проверка существует (Credo-чек, тест,
  шаг `make`); отсутствие проверки не проговаривается;
- проза — не длиннее 100 символов в строке; таблицы и содержимое fenced-блоков — исключение;
- ссылка внутри свода — `` `13-repos.md` ``; на свод приложения — `` `16-caching.md` ``
  с пометкой «(свод приложения)»;
- последний H2 файла — `## Связанные правила`.

Проверяется: `make rules-check` (`scripts/rules_lint.exs`).

## Как добавлять правило

- Правило живёт в **одном** файле; в остальных — строка-ссылка. Дублирование текста
  MUST NOT: копии расходятся при правке одной из них.
- Дом правила выбирается по слою, к которому оно относится, а не по тому, откуда
  вспомнилось: тест — `19-testing.md`, стиль и именование — `20-agreements.md`,
  persist — `13-repos.md`.
- Новое правило MUST приносить с собой либо пример «плохо / хорошо», либо строку
  `Проверяется: …` — иначе оно нечитаемо в разборе конкретной правки.
- Новый файл свода: имя `NN-<тема>.md`, шапка и хвост по шаблону, строка в карте выше **и**
  в карте `CLAUDE.md`, скилл `.claude/skills/<тема>/SKILL.md`. Номер берётся свободный
  с учётом номеров свода приложения.
- Отступление, которое не чинится, — строка в `DEBT.md` с причиной, а не молчаливое
  расхождение кода со сводом.

## Связанные правила

- Границы библиотеки — `10-architecture.md`
- Соглашения по коду — `20-agreements.md`
