---
name: errors
description: "Свод правил ошибок библиотеки Core: %Error{kind: :domain | :app} с ns/code/message/detail, конструкторы Error.domain/Error.app, контракт каталога <Aggregate>.Errors (коды, которые сборка репозиториев проверяет по errors:), ns и коды, которые ставит библиотека, классификация доменное/прикладное/ошибка программиста, матрица категорий, cause-цепочки (wrap, has?, format_chain), множество ошибок (поле errors, Error.many/1,2, вывод kind по слабейшему звену, плоский непустой состав, контейнер не бывает причиной, граница обхода, Error.messages/1, Result.traverse_all/2 и заворачивание списка на call site, состав клиенту только на :domain_error), защита секретов в detail и логах (что помечать sensitive, структуры и колонки, чек-лист). Использовать при заведении кода ошибки, правке каталога ошибок, выборе ns, сборке множества независимых отказов и batch-проходе, обработке ошибок на границе и работе с чувствительными данными."
---

# 12-errors.md

Прочитай `docs/rules/12-errors.md` целиком перед правкой и следуй ему: свод обязателен,
пересказ по памяти не годится.

Осознанные отступления от сводов — `docs/rules/DEBT.md`. Сверься с ним, прежде чем
«чинить» найденное несоответствие: часть из них оставлена намеренно.
