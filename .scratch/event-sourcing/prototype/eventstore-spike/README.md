# PROTOTYPE, wipe me

Одноразовый spike для тикета `.scratch/event-sourcing/issues/13-task-eventstore-spike.md`.
БД: `eventstore_spike_prototype_wipe_me`, `eventstore_spike_public_prototype_wipe_me` (порт 5433, `make infra-up`).
Результаты — `RESULTS.md`, сырой вывод — `out/`.
