# 03: `Result.traverse_all/2`

**What to build:** batch-проход, собирающий все провалы вместо остановки на первом.

**Blocked by:** 01

**Status:** ready-for-agent

**Spec:** [Множество ошибок](../spec.md) — «Комбинатор»

- [ ] `Result.traverse_all(list, fun)` → `{:ok, [b]} | {:error, [Error.t()]}`: проходит список
      целиком, порядок значений и провалов — порядок входа. Рядом с `traverse/2`, та же форма
      аргументов.
- [ ] `@doc` говорит, что голый список наружу не отдаётся: заворачивается в `Error.many` в теле
      той же функции; пример — в `@doc`.
- [ ] `traverse/2` не меняется.
- [ ] Тесты: пустой список, все успешны, все провалены, часть провалена, порядок, reason не
      `%Error{}` (собирается как есть).
