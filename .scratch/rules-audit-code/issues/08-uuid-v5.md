# 08: UUIDv5 в `Core.Prim.UUID`

**Status:** needs-triage

**What to build:** `version: 5` в `Core.Prim.UUID` (namespace опцией, конструктор от имени, составное имя).

**Why:** `app/13-repos.md`, «Уникальность без индекса состояния» требует детерминированный UUIDv5 как id
потока и конструктор в Prim идентификатора; `Core.Prim.UUID` допускает версии 1, 4, 7, и приложение
выключает проверку версии и зовёт транзитивную зависимость `:uuid` напрямую.

- [ ] опция и тесты; ссылка из `app/13`; CHANGELOG «Новое»
