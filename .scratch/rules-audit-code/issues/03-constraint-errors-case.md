# 03: Case-модуль проверки `constraint_errors`

**Status:** done

**What to build:** `use Core.Repo.ConstraintErrorsCase, otp_app:` — проверки по сгенерированным
`__constraint_errors__/0` / `__children_constraint_errors__/0` из `19-testing.md`, «`constraint_errors`»
(включая «read-репозиторий не объявляет `constraint_errors`»).

**Why:** функции генерируются ради этого теста, а сам тест пишет каждое приложение (копии почти идентичны).

- [x] case-модуль и тест; строка ратчета в `app/19-testing.md` — на него; CHANGELOG «Новое»

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** `use Core.Repo.ConstraintErrorsCase, otp_app:` — сверка `constraint_errors:` всех репозиториев
приложения с `changeset/2` и с БД

**Current behavior:**
`use Core.Repo.Pg` генерирует `__constraint_errors__/0`, `use Core.Repo.Pg.StateStored` —
`__children_constraint_errors__/0`; их moduledoc отсылает к тесту приложения. Пять проверок описаны в
`docs/rules/19-testing.md`, «`constraint_errors`», включая «read-репозиторий `constraint_errors:` не объявляет».
Тест пишет каждое приложение: две копии с одной логикой, у одной — списки исключений без записи в DEBT.
В библиотеке теста нет, у тестовых фикстур репозиториев нет ограничений.

**Desired behavior:**
- `use Core.Repo.ConstraintErrorsCase, otp_app: :my_app` генерирует пять тестов из `19-testing.md` по всем модулям
  `otp_app:` с `__constraint_errors__/0` — без перечня репозиториев: новый репозиторий попадает под проверку сам.
- Логика — публичные `check_*` (`:ok | {:error, detail}`), тест — `assert :ok = …`; провал печатает репозиторий
  и расхождение.
- `otp_app:` — обязательная опция с проверкой на компиляции (не `Core.Config.otp_app/0`: запрещён
  `make boundary-check`); `async:` — необязательная; sandbox checkout на `Core.Config.dao/0`.
- moduledoc `Core.Repo.Pg` и `Core.Repo.Pg.StateStored` ссылаются на case-модуль, а не на тест приложения.

**Key interfaces:**
- `Core.Repo.ConstraintErrorsCase` — новый: `__using__/1`, публичные `check_*`
- `__constraint_errors__/0`, `__children_constraint_errors__/0` — источник данных, форма не меняется

**Acceptance criteria:**
- [x] фикстура репозиториев с ограничениями (unique, FK, дочерняя таблица `children:`) и миграция тестовой БД;
      case-модуль на них проходит
- [x] негативные тесты каждой из пяти `check_*` на явном списке модулей
- [x] `19-testing.md`, «`constraint_errors`», `app/19-testing.md` (строка ратчета), `app/13-repos.md` — на
      case-модуль
- [x] CHANGELOG «Новое»: было — свой тест, стало — `use Core.Repo.ConstraintErrorsCase, otp_app: …`
- [x] `make` проходит

**Out of scope:**
- опция исключений (`except:`): добавляется отдельно и без поломки контракта, если понадобится
- ограничения таблиц вне `Repo.Pg` (резервы уникальных ключей — тикет 09)
- ограничения, которые `changeset/2` добавляет по условию на атрибуты

## Реализация

- `Core.Repo.ConstraintErrorsCase`: пять тестов — пять блоков модуля (`check_mapping_declared/1`,
  `check_constraints_mapped/1`, `check_constraint_names/2`, `check_children_foreign_keys/2`, `check_read_repos/1`);
  репозитории — `repos!/1` в `setup_all`. Незагруженное приложение и приложение без репозиториев — провал всех
  тестов модуля: иначе `otp_app:` на чужое загруженное приложение даёт холостой зелёный прогон.
- Отбор модулей — `Code.ensure_loaded?/1` + `function_exported?/3`: без загрузки ещё не тронутый репозиторий
  из `test/support` молча считался read и выпадал из сверок 1–4 (поймал тест `check_read_repos/1`).
- Сверки 3 и 4 — только по write-репозиториям; каталог — через `Core.Config.dao/0` по `__schema__(:source)`
  без префикса. `repo:` у write-репозитория и `@schema_prefix` не поддержаны (у потребителей их нет) —
  ограничение записано в moduledoc.
- Имя ограничения сверяется точно; `match: :prefix | :suffix` и `Regex` не поддержаны (у потребителей нет).
- Фикстура: `Core.ConstraintErrorsFixture` (unique-индекс, FK, check, дочерняя таблица с FK на справочник,
  read-репозиторий на своей схеме без `changeset/2`), миграция `20260917130000`. Самопроверка —
  `Core.Repo.ConstraintErrorsTest` на `otp_app: :core`; мутация фикстуры (убран `check:` и имя ребёнка)
  валит три теста из пяти.
- Побочный шум: `test/core/es/projection_test.exs` катит миграцию с фиксированной версией `20260914150000`,
  и Ecto предупреждает, что более новая уже применена; даёт любая новая миграция, тест проходит.
