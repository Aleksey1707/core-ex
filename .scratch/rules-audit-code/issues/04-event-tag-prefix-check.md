# 04: Префикс тега события — проверка сборкой

**Status:** needs-triage

**What to build:** `use Core.Es.Event.Codec` даёт `CompileError`, если тег из `tags:` или ключ / значение
`upcasts:` не начинается с `type <> "."`. Уникальность `type:` среди плагинов фасада уже проверяется —
глобальная уникальность тега следует из неё.

**Why:** `app/14-events-outbox.md`, «Wire-тег события» держит уникальность тегов между агрегатами ратчетом
приложения; библиотека префикс с `type:` не сверяет (`lib/core/es/event/codec.ex`).

- [ ] проверка и тесты; ратчет в `app/19` / `app/14` → «Проверяется: `CompileError`»; CHANGELOG
      «Изменения контракта макросов»
