# 09: Изменяемый уникальный ключ event-sourced агрегата

**Status:** needs-triage

**What to build:** норма (и, возможно, DDL в `Core.Es.Migration` / контракт в `Core.*`) для изменяемого
уникального ключа event-sourced агрегата: таблица резервов `(scope, key) → aggregate_id`, резерв в транзакции
до `append`, `ON CONFLICT DO NOTHING` с проверкой числа строк, идемпотентность при повторе.

**Why:** `app/13-repos.md`, «Уникальность без индекса состояния» покрывает только неизменяемый ключ; приложение
решило изменяемый у себя (логин, имя роли), ничего специфичного для него в решении нет.
